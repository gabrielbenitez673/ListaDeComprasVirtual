import 'package:flutter/material.dart';

class AppTheme {
  //Colores principales de la paleta "Fresca"
  static const Color primaryBlue = Color(0xFFA8DADC);
  static const Color primaryMint = Color(0xFF45B8AC);

  //Colores de fondo y neutros
  static const Color background = Color(0xFFF0F0F0);
  static const Color white = Color(0xFFFFFFFF);

  //colores secundarios para textos y detalles
  static const Color textDark = Color(0xFF333333);
  static const Color textLight = Color(0xFF757575);

  //Color de acento para errores o alertas (opcional)
  static const Color accentCoral = Color(0xFFE63946);
}
