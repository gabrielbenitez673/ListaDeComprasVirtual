import 'package:flutter/material.dart';

class MainDrawer extends StatelessWidget {
  const MainDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    //creamos el drawer para la navegacion del historial de compras
    //realizadas anteriormente (es como un historico de compras en el tiempo)
    return const Drawer();
  }
}
